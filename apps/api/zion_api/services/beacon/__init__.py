"""Beacon: one deterministic, typed, human-approved demonstration workflow.

Every input is a canned synthetic fixture; there are no model SDKs, live
feeds, queues, or paid calls anywhere in this package. The engine may propose
an allocation but can never purchase, dispatch, rank beneficiaries, order an
evacuation, or publish a warning -- those capabilities simply do not exist in
the typed tool registry.
"""
